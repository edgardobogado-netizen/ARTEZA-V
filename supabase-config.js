// supabase-config.js
// Rellena estos valores con los de tu proyecto Supabase (Panel → Settings → API).
// IMPORTANTE: usa únicamente la ANON KEY para el cliente (browser).
// Nunca pongas la SERVICE_ROLE KEY en el frontend.

window.SUPABASE_URL = "https://<TU-PROYECTO>.supabase.co";
window.SUPABASE_ANON_KEY = "<TU_ANON_KEY>";

console.info("supabase-config.js cargado — sustituye los placeholders por tus credenciales.");